# 🌐 API Directory

This directory is reserved for the web interface and API endpoints of the AI Hedge Fund.

## 📂 Future Implementation

Currently, the project is primarily executed via `main.py`. This directory is intended to house FastAPI or similar framework code to expose the fund's analysis and execution capabilities as a RESTful service.
